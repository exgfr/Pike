import Config

# Test-specific Pike configuration
config :pike, []
# Test settings can override defaults
# store: Pike.Store.ETS,
# assign_to: :pike_api_key,
# on_auth_failure: {Pike.Responder.Default, :auth_failed}
